#[tokio::main]
async fn main() {
    /*
    pub trait builder {
        pub fn builders<T>(){
    
        }
    }

 */
    let a = avenue::restful::client::test().await;
    while true {
        continue
    }
}
